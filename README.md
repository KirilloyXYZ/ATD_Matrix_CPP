# АТД Matrix C++

Лабораторная работа №3: полиморфный абстрактный тип данных для матриц на основе АТД последовательности из лабораторной работы №2.

## Возможности

- Абстрактный интерфейс `Matrix<T>`.
- Реализации `RectangularMatrix<T>`, `SquareMatrix<T>`, `DiagonalMatrix<T>` и `TriangularMatrix<T>`.
- Компактное хранение специальных матриц: только главная диагональ для диагональной матрицы и "рваные строки" для верхней/нижней треугольной матрицы.
- Операции над матрицами: сложение, умножение на скаляр, норма Фробениуса.
- Элементарные преобразования строк и столбцов.
- Решение систем линейных уравнений:
  - метод Гаусса без выбора ведущего элемента;
  - метод Гаусса с выбором ведущего элемента;
  - LU-разложение.
- Консольный интерфейс для создания матриц, выполнения операций, замера производительности и решения СЛАУ.
- Модульные тесты для последовательностей, битовых последовательностей, матриц и решателей СЛАУ.

## Стоимость реализованного варианта

По таблице вариантов задания реализованы следующие пункты:

| Пункт | Реализация | Рейтинг |
| --- | --- | --- |
| `C-7.1.1` | "Обычная" матрица, хранение всех элементов | `5` |
| `C-7.2.1` | Квадратная матрица | `5` |
| `C-7.2.2` | Прямоугольная матрица | `5` |
| `C-7.2.3` | Треугольная матрица | `5` |
| `C-7.2.4` | Диагональная матрица | `4` |

Итого: `24` балла, что больше минимально требуемых `12` баллов.

## Сборка

```powershell
mingw32-make all
```

## Запуск программы

```powershell
.\lab3_app.exe
```

## Запуск тестов

```powershell
mingw32-make run_tests
```

Ожидаемый результат:

```text
All tests passed!
```

## Сложность

Обозначения:

- `m` - количество строк матрицы;
- `n` - количество столбцов матрицы;
- для СЛАУ `n` - размер системы;
- цикломатическая сложность считается как `1 + количество точек ветвления` внутри тела функции.

### Операции над матрицами

| Операция | Временная сложность | Дополнительная память | Цикломатическая сложность |
| --- | --- | --- | --- |
| `Get`, `Set` | `O(1)` | `O(1)` | `1` |
| `Add` | `O(m * n)` | `O(m * n)` | `3` |
| `MultiplyByScalar` | `O(m * n)` | `O(m * n)` | `3` |
| `Norm` | `O(m * n)` | `O(1)` | `3` |
| `SwapRows` | `O(n)` | `O(n)` | `2` |
| `SwapColumns` | `O(m)` | `O(1)` | `3` |
| `MultiplyRow` | `O(n)` | `O(1)` | `2` |
| `MultiplyColumn` | `O(m)` | `O(1)` | `2` |
| `AddRowMultiple` | `O(n)` | `O(1)` | `2` |
| `AddColumnMultiple` | `O(m)` | `O(1)` | `2` |

### Специальные матрицы

Для `DiagonalMatrix<T>` используется обозначение `n` - размер квадратной матрицы. Хранятся только `n` элементов главной диагонали.

| Операция | Временная сложность | Дополнительная память | Цикломатическая сложность |
| --- | --- | --- | --- |
| `Get` | `O(1)` | `O(1)` | `2` |
| `Set` | `O(1)` | `O(1)` | `3` |
| `Add` с диагональной матрицей | `O(n)` | `O(n)` | `5` |
| `Add` с другой матрицей | `O(n^2)` | `O(n^2)` | `5` |
| `MultiplyByScalar` | `O(n)` | `O(n)` | `2` |
| `Norm` | `O(n)` | `O(1)` | `2` |
| `SwapRows`, `SwapColumns` | `O(1)` | `O(1)` | `2` |
| `MultiplyRow`, `MultiplyColumn` | `O(1)` | `O(1)` | `1` |
| `AddRowMultiple`, `AddColumnMultiple` | `O(1)` | `O(1)` | `3` |

Для `TriangularMatrix<T>` используется обозначение `n` - размер квадратной матрицы. Хранятся только элементы верхней или нижней треугольной части: `n * (n + 1) / 2` элементов.

| Операция | Временная сложность | Дополнительная память | Цикломатическая сложность |
| --- | --- | --- | --- |
| `Get` | `O(1)` | `O(1)` | `2` |
| `Set` | `O(1)` | `O(1)` | `3` |
| `Add` | `O(n^2)` | `O(n^2)` | `6` |
| `MultiplyByScalar` | `O(n^2)` | `O(n^2)` | `3` |
| `Norm` | `O(n^2)` | `O(1)` | `3` |
| `SwapRows`, `SwapColumns` | `O(1)` | `O(1)` | `2` |
| `MultiplyRow` | `O(n)` | `O(1)` | `2` |
| `MultiplyColumn` | `O(n)` | `O(1)` | `3` |
| `AddRowMultiple` | `O(n)` | `O(1)` | `3` |
| `AddColumnMultiple` | `O(n)` | `O(1)` | `4` |

Операции, которые нарушают структуру диагональной или треугольной матрицы, завершаются исключением. Если результат сложения двух специальных матриц не обязан сохранять специальный вид, возвращается обычная `SquareMatrix<T>`.

### Решатели СЛАУ

| Функция | Временная сложность | Дополнительная память | Цикломатическая сложность |
| --- | --- | --- | --- |
| `MultiplyMatrixByVector` | `O(m * n)` | `O(m)` | `3` |
| `VectorNorm` | `O(n)` | `O(1)` | `2` |
| `ComputeResidualNorm` | `O(m * n)` | `O(m)` | `2` |
| `ComputeRelativeError` | `O(n)` | `O(n)` | `3` |
| `SolveGaussNoPivot` | `O(n^3)` | `O(n^2)` | `4` |
| `SolveGaussPartialPivot` | `O(n^3)` | `O(n^2)` | `7` |
| `DecomposeLU` | `O(n^3)` | `O(n^2)` | `6` |
| `ForwardSubstitution` | `O(n^2)` | `O(n)` | `3` |
| `BackwardSubstitution` | `O(n^2)` | `O(n)` | `3` |
| `SolveLU` | `O(n^3)` | `O(n^2)` | `1` |

`SolveLU` имеет цикломатическую сложность `1`, потому что это функция-обертка: ветвления и циклы находятся внутри `DecomposeLU`, `ForwardSubstitution` и `BackwardSubstitution`.

## Очистка сборочных файлов

```powershell
mingw32-make clean
```
